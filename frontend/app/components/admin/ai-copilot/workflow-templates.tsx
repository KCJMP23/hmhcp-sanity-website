'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function WorkflowTemplates() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Workflow Templates</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Workflow templates for healthcare content types will be implemented in Task 2</p>
      </CardContent>
    </Card>
  );
}